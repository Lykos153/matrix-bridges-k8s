---
apiVersion: v1
kind: Service
metadata:
  name: mautrix-instagram-bridge
  labels:
    app.kubernetes.io/name: mautrix-instagram
    app.kubernetes.io/instance: matrix-instagram
spec:
  type: ClusterIP
  externalTrafficPolicy: 
  ports:
    - port: 29318
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: mautrix-instagram
    app.kubernetes.io/instance: matrix-instagram
